---
name: format
ndescription: Format code with prettier for AuraQuest
usage: /format
---

Runs Prettier with AuraQuest's configuration (when available).
Currently the project doesn't use Prettier - code is manually formatted.

```bash
npx prettier --write "*.js" "*.css" "*.html"
```
